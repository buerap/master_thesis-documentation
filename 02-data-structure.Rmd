# Data structure

Here is a review of existing methods (and I try a test here).
The tables are available under `tbl(db, "helle")`
Zunächst laden wir `` und 

## Observational study

```{r test-table, tidy=FALSE}
knitr::kable(
  head(tbl.test, 20), caption = 'Here is a TEST table!',
  booktabs = TRUE
)
```

