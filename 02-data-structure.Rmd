# Data structure

Here, all raw data files are described. The tables are available under `https://github.com/buerap/master_thesis`.

## Observational study
The described columns are available in the raw data file `raw_pos_characteristics.csv`, which contains information on the field sites.
```{r tidy = FALSE, echo = FALSE}
knitr::kable(
  head(tbl.test, 20),
  booktabs = TRUE
)
```

